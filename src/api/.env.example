# Required in every environment.
APP_ENV=development

# Development defaults to this path when omitted.
# Production must set an absolute path outside the repository.
SQLITE_DB_PATH=../var/dev.sqlite3

# Production example:
# APP_ENV=production
# SQLITE_DB_PATH=/var/lib/ecommerce/production.sqlite3
